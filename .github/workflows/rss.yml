name: Generate Feeds

on:
  workflow_dispatch:
  repository_dispatch:
    types: [external-trigger]
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      changes: ${{ steps.compare.outputs.changes }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Clear cache
        run: rm -rf cache/*

      - name: Generate feeds (local only)
        run: npm run static

      # ---------------------------------------------------------
      # 🔥 CONFRONTO GUID
      # ---------------------------------------------------------
      - name: Compare latest GUIDs
        id: compare
        run: |
          echo "Checking for updates..."
          CHANGED=0

          for FEED in grsicilia gr3 primapagina radio3mondo; do
            echo "Checking $FEED..."

            curl -s https://ric-82.github.io/raiplaysound-feed/rss/programmi/$FEED.xml -o old_$FEED.xml || true

            OLD_GUID=$(grep -m1 "<guid" old_$FEED.xml | sed -e 's/.*<guid[^>]*>//' -e 's/<\/guid>.*//')
            NEW_GUID=$(grep -m1 "<guid" out/rss/programmi/$FEED.xml | sed -e 's/.*<guid[^>]*>//' -e 's/<\/guid>.*//')

            echo "OLD: $OLD_GUID"
            echo "NEW: $NEW_GUID"

            [ "$OLD_GUID" != "$NEW_GUID" ] && CHANGED=1
          done

          echo "changes=$CHANGED" >> $GITHUB_OUTPUT

      # ---------------------------------------------------------
      # 🔥 COMMIT SOLO SE CI SONO NOVITÀ
      # ---------------------------------------------------------
      - name: Commit updated cache
        if: steps.compare.outputs.changes == '1'
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add -f cache/
          git restore --staged README.md
          git diff --cached --quiet || git commit -m "Update cache"
          git push

      # ---------------------------------------------------------
      # 🔥 UPLOAD ARTIFACT SOLO SE CI SONO NOVITÀ
      # ---------------------------------------------------------
      - name: Upload Pages artifact
        if: steps.compare.outputs.changes == '1'
        uses: actions/upload-pages-artifact@v5
        with:
          path: out

  deploy:
    needs: build
    if: needs.build.outputs.changes == '1'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v5
